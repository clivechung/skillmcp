services:
  nginx:
    image: nginx:alpine
    container_name: skill-mcp-local-ingress
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - skill-mcp-app
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/healthz"]
      interval: 10s
      timeout: 3s
      retries: 3

  skill-mcp-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: skill-mcp-local-app
    ports:
      - "8000:8000"
    volumes:
      - ./skills:/app/skills:ro
      - ./src:/app/src:ro
    environment:
      - SKILL_MCP_LOG_LEVEL=DEBUG
      - SKILL_MCP_HOST=0.0.0.0
      - SKILL_MCP_PORT=8000
      - SKILL_MCP_SKILLS_DIR=/app/skills

